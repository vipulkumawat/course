version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"
    environment:
      - KUBERNETES_SERVICE_HOST=kind-control-plane
    volumes:
      - ./src:/app/src
      - ~/.kube:/root/.kube
    command: python src/api/server.py
