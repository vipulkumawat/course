version: '3.8'

services:
  incident-response:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
    environment:
      - PYTHONPATH=/app
    command: python -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload
