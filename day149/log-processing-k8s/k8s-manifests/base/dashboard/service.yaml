apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: log-processing
spec:
  type: LoadBalancer
  selector:
    app: dashboard
  ports:
  - port: 3000
    targetPort: 3000
    name: http
